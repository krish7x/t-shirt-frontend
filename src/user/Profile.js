import React from "react";
import Base from "../core/Base";

const Profile = () => {
	return (
		<Base title='Profile'>
			<h1>This page is user profile</h1>
		</Base>
	);
};

export default Profile;
